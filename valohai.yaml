- step:
    name: preprocess
    image: dusanzamahajev/python-java:0.1
    command:
    - pip install -r requirements.txt --user
    - python preprocess.py
    inputs:
      - name: train
        filename: train.csv
      - name: test
        filename: test.csv
        optional: true